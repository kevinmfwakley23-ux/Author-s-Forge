{
  "name": "authors-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Author's Forge — durable, local-first AI publishing studio foundation.",
  "scripts": {
    "build": "rm -rf .forge-build && tsc -p tsconfig.json && rm -rf dist && cp -R .forge-build dist && cp -R public dist/public",
    "test": "npm run build && node --test test/**/*.test.js",
    "check": "npm run build && npm test",
    "verify": "npm run build && node --test test/**/*.test.js && npm run completion && npm run test:browser && npm run test:browser:mobile",
    "studio": "npm run build && node dist/studio-server.js",
    "completion": "node scripts/forge-completion.js",
    "browser:install": "playwright install chromium",
    "test:browser": "npm run build && node scripts/studio-browser-acceptance.js",
    "test:browser:mobile": "npm run build && node scripts/studio-mobile-acceptance.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.0.0",
    "typescript": "5.9.3"
  }
}
