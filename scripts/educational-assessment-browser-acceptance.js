#!/usr/bin/env node
const assert=require("node:assert/strict"),{spawn}=require("node:child_process"),{mkdtemp,rm}=require("node:fs/promises"),{tmpdir}=require("node:os"),{join}=require("node:path"),{chromium}=require("@playwright/test");const HOST="127.0.0.1",PORT=7000+Math.floor(Math.random()*100),projectId=`assessment-browser-${Date.now()}`,sleep=ms=>new Promise(r=>setTimeout(r,ms));async function wait(base,t=12000){const s=Date.now();while(Date.now()-s<t){try{if((await fetch(`${base}/api/health`)).ok)return}catch{}await sleep(100)}throw new Error(`Timed out waiting for ${base}`)}async function api(base,path,o={}){const r=await fetch(base+path,{...o,headers:{"content-type":"application/json",...(o.headers||{})}}),j=await r.json().catch(()=>({}));assert.equal(r.ok,true,`${o.method||"GET"} ${path}: ${JSON.stringify(j)}`);return j}function start(dataDir){return spawn(process.execPath,["dist/educational-workbook-server.js"],{env:{...process.env,HOST,WORKBOOK_PORT:String(PORT),FORGE_DATA_DIR:dataDir},stdio:["ignore","pipe","pipe"]})}async function stop(c){if(!c||c.exitCode!==null)return;c.kill("SIGTERM");await Promise.race([new Promise(r=>c.once("exit",r)),sleep(2000)]);if(c.exitCode===null)c.kill("SIGKILL")}
async function main(){const dataDir=await mkdtemp(join(tmpdir(),"forge-assessment-browser-"));let server=start(dataDir),browser;try{const base=`http://${HOST}:${PORT}`;await wait(base);await api(base,"/api/projects",{method:"POST",body:JSON.stringify({id:projectId,title:"Assessment Browser Acceptance"})});browser=await chromium.launch({executablePath:process.env.FORGE_BROWSER_EXECUTABLE||chromium.executablePath(),headless:true,args:["--no-sandbox","--disable-gpu"]});const context=await browser.newContext({viewport:{width:390,height:844},deviceScaleFactor:2,isMobile:true,hasTouch:true}),page=await context.newPage();await page.goto(`${base}/educational-assessment.html?project=${encodeURIComponent(projectId)}`,{waitUntil:"networkidle"});await page.waitForFunction(()=>document.querySelector('#status')?.textContent.includes('Assessment Browser Acceptance'));const overflow=await page.evaluate(()=>({viewport:document.documentElement.clientWidth,body:document.body.scrollWidth,doc:document.documentElement.scrollWidth}));assert.ok(overflow.body<=overflow.viewport+1&&overflow.doc<=overflow.viewport+1,`Assessment workspace overflows: ${JSON.stringify(overflow)}`);await page.locator('#rubric-form [name="title"]').fill('Fraction Reasoning Rubric');await page.locator('#rubric-form [name="criteria"]').fill('Accuracy | 60 | Compare fractions accurately | Uses a valid comparison strategy and conclusion | Look for a correct representation and conclusion.\nReasoning | 40 | Explain fraction comparison reasoning | Explains why the comparison is valid | Look for clear reasoning in the selected response mode.');const created=page.waitForResponse(r=>r.url().endsWith('/workbooks/assessment/rubrics')&&r.request().method()==='POST');await page.locator('#rubric-form button[type="submit"]').tap();assert.equal((await created).ok(),true);await page.waitForFunction(()=>document.querySelector('#rubrics')?.textContent.includes('Fraction Reasoning Rubric'));const rubrics=await api(base,`/api/projects/${projectId}/workbooks/assessment/rubrics`);assert.equal(rubrics.rubrics.length,1);assert.deepEqual(rubrics.rubrics[0].allowedResponseModes,["written","oral","diagram"]);await page.locator('#score-form [name="activityOrTaskId"]').fill('fraction-performance-1');await page.locator('#response-mode').selectOption('oral');const cards=page.locator('[data-score-criterion]');assert.equal(await cards.count(),2);await cards.nth(0).locator('[data-level]').selectOption('proficient');await cards.nth(0).locator('[data-evidence]').fill('Correctly compared both fractions aloud.');await cards.nth(1).locator('[data-level]').selectOption('advanced');await cards.nth(1).locator('[data-evidence]').fill('Transferred the reasoning to a new pair.');await page.locator('#score-form [name="feedback"]').fill('Accurate oral reasoning with strong transfer.');const scored=page.waitForResponse(r=>r.url().endsWith('/workbooks/assessment/records')&&r.request().method()==='POST');await page.locator('#score-form button[type="submit"]').tap();assert.equal((await scored).ok(),true);await page.waitForFunction(()=>document.querySelector('#score-result')?.textContent.includes('80%'));let records=await api(base,`/api/projects/${projectId}/workbooks/assessment/records`);assert.equal(records.records[0].weightedPercent,80);assert.equal(records.records[0].masteryBand,'proficient');assert.equal(records.records[0].responseMode,'oral');await stop(server);server=start(dataDir);await wait(base);records=await api(base,`/api/projects/${projectId}/workbooks/assessment/records`);assert.equal(records.records.length,1);assert.equal(records.records[0].id,'assessment-001');await page.reload({waitUntil:'networkidle'});await page.waitForFunction(()=>document.querySelector('#history')?.textContent.includes('fraction-performance-1'));await context.close();console.log('EDUCATIONAL ASSESSMENT BROWSER ACCEPTANCE PASSED: weighted rubric + oral evidence + deterministic 80% score + restart persistence + Android fit.')}finally{if(browser)await browser.close().catch(()=>{});await stop(server).catch(()=>{});await rm(dataDir,{recursive:true,force:true})}}main().catch(e=>{console.error(e.stack||e);process.exitCode=1});
