import {applyAuthorOverride,lockCanon,resolveAuthorControl,type AuthorDecision} from "../domain/author-control";
export class AuthorControlService{constructor(private readonly projectId:string,private decisions:AuthorDecision[]=[]){if(!projectId.trim())throw new Error("Project id is required.");} suggest(targetId:string,content:string,reason:string):AuthorDecision{return this.add({targetId,content,reason,status:"ai-suggestion"});} draft(targetId:string,content:string,reason:string):AuthorDecision{return this.add({targetId,content,reason,status:"ai-draft"});} approve(targetId:string,content:string,reason="Author approved this state."):AuthorDecision{return this.add({targetId,content,reason,status:"author-approved"});} override(targetId:string,content:string,reason="Author override takes precedence over prior recommendations."):AuthorDecision{this.decisions=applyAuthorOverride(this.decisions,{projectId:this.projectId,targetId,content,reason});return this.decisions.at(-1)!;} lock(targetId:string,content:string,reason?:string):AuthorDecision{this.decisions=lockCanon(this.decisions,{projectId:this.projectId,targetId,content,reason});return this.decisions.at(-1)!;} resolve(targetId:string):AuthorDecision|undefined{return resolveAuthorControl(this.decisions,this.projectId,targetId);} history():readonly AuthorDecision[]{return this.decisions.map(d=>({...d}));} private add(input:{targetId:string;content:string;reason:string;status:"ai-suggestion"|"ai-draft"|"author-approved"}):AuthorDecision{const d={id:`decision_${Date.now()}_${Math.random().toString(36).slice(2,9)}`,projectId:this.projectId,targetId:input.targetId,status:input.status,content:input.content,reason:input.reason,createdAt:new Date().toISOString(),supersedesId:this.resolve(input.targetId)?.id};this.decisions=[...this.decisions,d];return d;}}
