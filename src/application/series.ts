import {addBookToSeries,addSeriesTimelineEvent,validateSeriesState,type SeriesState,type SeriesTimelineEvent} from "../domain/series";
export class SeriesService{constructor(private readonly projectId:string,private state:SeriesState){if(state.projectId!==projectId)throw new Error("Series belongs to another project.");validateSeriesState(state);} getState():SeriesState{return validateSeriesState(this.state);} addBook(bookId:string):SeriesState{this.state=addBookToSeries(this.state,bookId);return this.getState();} addTimelineEvent(event:SeriesTimelineEvent):SeriesState{this.state=addSeriesTimelineEvent(this.state,event);return this.getState();}}
