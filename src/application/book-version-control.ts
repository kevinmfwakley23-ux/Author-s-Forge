import {branchVersion,compareBookVersions,mergeVersions,rollbackVersion,validateBookSnapshot,type BookSnapshot,type BookVersionBranch,type BookVersionComparison,type BookVersionHistory} from "../domain/book-version-control";
export class BookVersionControlService{constructor(private readonly history:BookVersionHistory){} snapshot(v:BookSnapshot):BookSnapshot{if(v.projectId!==this.history.projectId||v.bookId!==this.history.bookId)throw new Error("Version belongs to another project or book.");return validateBookSnapshot(v);} compare(fromId:string,toId:string):BookVersionComparison{const f=this.find(fromId),t=this.find(toId);return compareBookVersions(f,t);} restore(versionId:string):BookSnapshot{return rollbackVersion(this.history,versionId);} createBranch(name:string,baseVersionId:string):BookVersionBranch{return branchVersion(this.history,{name,baseVersionId});} merge(targetId:string,sourceId:string,baseId:string):BookSnapshot{return mergeVersions(this.find(targetId),this.find(sourceId),this.find(baseId));} private find(id:string):BookSnapshot{const v=this.history.versions.find(x=>x.id===id);if(!v)throw new Error(`Version "${id}" was not found.`);return v;}}
