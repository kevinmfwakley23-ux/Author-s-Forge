export const DELIVERY_AUDIT_FORMAT_VERSION=1 as const;
export const DELIVERY_AUDIT_CATEGORIES=["canon","continuity","timeline","character","pov","style","grammar","formatting","research","artwork","cover","metadata","publishing"] as const;
export type DeliveryAuditCategory=typeof DELIVERY_AUDIT_CATEGORIES[number];
export type DeliveryAuditSeverity="critical"|"warning"|"info";
export interface DeliveryAuditCheck { readonly id:string; readonly category:DeliveryAuditCategory; readonly passed:boolean; readonly severity:DeliveryAuditSeverity; readonly message:string; readonly remediation?:string; }
export interface DeliveryAuditReport { readonly formatVersion:typeof DELIVERY_AUDIT_FORMAT_VERSION; readonly projectId:string; readonly generatedAt:string; readonly checks:readonly DeliveryAuditCheck[]; readonly status:"ready-for-author-approval"|"attention"|"blocked"; readonly passedCount:number; readonly attentionCount:number; }
export function createDeliveryAuditReport(input:{projectId:string;checks:readonly DeliveryAuditCheck[];generatedAt?:string}):DeliveryAuditReport { if(!input.projectId.trim())throw new Error("Project id is required."); const ids=new Set<string>(); for(const c of input.checks){if(!c.id.trim())throw new Error("Audit check id is required.");if(ids.has(c.id))throw new Error(`Duplicate audit check id "${c.id}".`);ids.add(c.id);if(!DELIVERY_AUDIT_CATEGORIES.includes(c.category))throw new Error(`Unsupported audit category "${c.category}".`);if(!c.message.trim())throw new Error("Audit check message is required.");} const checks=input.checks.map(c=>Object.freeze({...c})); const failed=checks.filter(c=>!c.passed); const critical=failed.some(c=>c.severity==="critical"); return Object.freeze({formatVersion:DELIVERY_AUDIT_FORMAT_VERSION,projectId:input.projectId,generatedAt:input.generatedAt??new Date().toISOString(),checks,passedCount:checks.filter(c=>c.passed).length,attentionCount:failed.length,status:critical?"blocked":failed.length?"attention":"ready-for-author-approval"}); }
export function validateDeliveryAuditReport(r:DeliveryAuditReport):DeliveryAuditReport { const rebuilt=createDeliveryAuditReport({projectId:r.projectId,checks:r.checks,generatedAt:r.generatedAt}); if(rebuilt.passedCount!==r.passedCount||rebuilt.attentionCount!==r.attentionCount||rebuilt.status!==r.status)throw new Error("Delivery audit summary is inconsistent."); return rebuilt; }
