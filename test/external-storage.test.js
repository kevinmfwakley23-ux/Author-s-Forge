const assert=require("node:assert/strict");const test=require("node:test");const{createProjectStorageBinding,MemoryStorageProvider,ExternalStorageService,createDownloadableProjectPackageFilename}=require("../.forge-build");
test("external storage keeps Forge project as source of truth",async()=>{const provider=new MemoryStorageProvider();const service=new ExternalStorageService(provider);const binding=createProjectStorageBinding({projectId:"project-1",providerId:"download"});assert.equal(binding.sourceOfTruth,"forge-project");await service.put(binding,"package.json",new TextEncoder().encode("{}"),"application/json");const bytes=await service.get(binding,"package.json");assert.equal(new TextDecoder().decode(bytes),"{}");assert.equal((await service.list(binding)).length,1);});
test("storage bindings reject provider mismatch and unsafe project source",()=>{const provider=new MemoryStorageProvider();const service=new ExternalStorageService(provider);assert.throws(()=>service.bind(createProjectStorageBinding({projectId:"p",providerId:"google-drive"})),/does not match/);assert.match(createDownloadableProjectPackageFilename("my-book"),/my-book\\.forge-project\\.json/);});
